{
  "name": "@agentistics/server",
  "version": "2.11.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run server/index.ts",
    "watch": "bun run server/otel-watcher.ts",
    "build:binary": "bun build --compile bin/cli.ts --outfile ../../release/agentop",
    "build:binary:windows": "bun build --compile --target=bun-windows-x64 bin/cli.ts --outfile ../../release/agentop.exe"
  },
  "dependencies": {
    "@agentistics/core": "workspace:*",
    "@agentistics/tui": "workspace:*",
    "jose": "^6.2.3",
    "mongodb": "^6.12.0"
  }
}
